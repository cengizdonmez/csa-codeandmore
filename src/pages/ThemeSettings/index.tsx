import React, { ReactElement } from 'react'

interface Props {
  
}

function ThemeSettings({}: Props): ReactElement {
  return (
    <div>
      <h1>Theme Settings</h1>
    </div>
  )
}

export default ThemeSettings
